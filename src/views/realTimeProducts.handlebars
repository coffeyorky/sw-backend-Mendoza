<ul class="dirigir">
      {{#each productos}}
<div class="caja">
    <img alt={{this.title}} src={{this.imagen}} width="200"/>
    <h2>{{this.title}}</h2>
    <h4>{{this.category}}</h4>
    <p>{{this.description}}</p>
    <h3>${{this.price}}</h3>
     </div>
    {{/each}}  
 </ul>

