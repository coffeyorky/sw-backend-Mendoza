 <h1>Lista de productos</h1>
 <ul class="dirigir">
      {{#each productos}}
<div class="caja">
    <h2>{{this.title}}</h2>
    <h4>{{this.category}}</h4>
    <p>{{this.description}}</p>
    <h3>{{this.price}}</h3>
     </div>
    {{/each}}  
 </ul>
 

 

